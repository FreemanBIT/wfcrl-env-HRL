# 风电场闭环控制开发方案（基于现有 WFCRL / FAST.Farm 工程）

> 面向 **NREL 5MW × 6 机组（2 行 3 列，行/列间距均 4D = 504 m）** 场站，在你现有的 WFCRL FAST.Farm 仿真工程之上，开发 A / B / C 三套闭环控制方案。
> 本文档只给出**开发计划**，供你逐个交付给 agent 实现。计划刻意区分 **三方案共用的基础模块** 与 **各方案独有的新增模块**，并标注每个模块挂接到现有工程的**具体文件与接口**。

---

## 0. 阅读指引与总体策略

- **交付顺序建议**：先做 **Phase 0（共用基础设施）**，再按 **A → B → C** 逐个开发。A 是最小闭环、验证管线；B 是主方案；C 是增量增强。三者复用同一套感知/接口/评估底座。
- **每个方案的三层结构统一**：`整体模型` → `观测器` → `控制器`。这与配套的《架构设计方案》《公式推导》两份文档一一对应，agent 实现时可回查公式编号。
- **本文档中的"模块"= 一个可独立开发、可单测的 Python 包/文件**。每个模块给出：职责、输入/输出、挂接点、验收标准。

---

## 1. 现有工程分析（开发前必须理解的接口）

我已核查你上传的工程 `wfcrl-env-HRL-main`，其 FAST.Farm 仿真通过一个**自定义文件桥接 DISCON**（`wfcrl/simulators/fastfarm/src/DISCON_bridge.f90`）与外部控制器通信。这是**三方案共同的落地接口**，务必先理解：

### 1.1 指令注入接口（控制器 → FAST.Farm）

- **文件**：运行目录下的 `controls.txt`，DISCON 桥每个仿真步读取一次。
- **格式**：
  ```
  step=<整数步号>
  T1 yaw=<deg> pitch=<deg> torque=<Nm> power=<MW>
  T2 yaw=<deg> ...
  ...
  T6 yaw=<deg> ...
  END
  ```
- **步号门控**：桥内 `applied_step` 记录已应用步号，仅当 `read_step > applied_step` 才应用新指令（避免重复施加）。**控制器每次下发必须递增 step**。
- **偏航执行**：桥内以比例速率施加 `yaw_rate = (cmd_yaw − nac_yaw)·0.2`（`avrSWap(48)`）。控制器给的是**目标偏航角（deg，场级坐标）**。
- **降功率（诱导因子）执行**：三选一，优先级 `power > torque > pitch`：
  - `power=<MW>`：桥内做功率闭环跟踪（前馈 `T_ff = P_target/ω` + PI 修正），**推荐用这个表达诱导因子指令**（等价于给定功率设定点）。
  - `torque=<Nm>`：直接转矩覆盖。
  - `pitch=<deg>`：集体变桨覆盖。
- **诱导因子 ↔ 指令换算**：控制器内部优化出的轴向诱导 `a_i`，需换算为功率设定点下发。换算关系见 §Phase0-M4。

### 1.2 量测导出接口（FAST.Farm → 控制器）

- **文件**：运行目录下 `measurements_T<id>.txt`，每机一份，桥每步覆写。
- **内容**（已确认字段）：
  ```
  step=<N> t=<s> genpwr=<kW> genspd=<rpm> gentq=<Nm> rotspd=<rpm>
  wind_x=<HorWindV, m/s>        # 机舱风速测量（关键观测量）
  blpitch=<deg> nacyaw=<deg>    # 桨距、机舱偏航
  mip1=<kNm> moop1=<kNm> mzb1=<kNm>   # 叶根弯矩（载荷监测）
  ```
- **传感器映射**：`wind_x`≈机舱风速计/雷达来流，`nacyaw`≈机舱朝向，`genpwr`≈SCADA 功率。这些正是三方案观测器的输入。

### 1.3 备用接口：ROSCO ZeroMQ

- 工程内也含 `ZeroMQInterface.f90`（ROSCO v2.9 风格），当前 `ZMQ_Mode = 0`（禁用）。它每 `n_DT_ZMQ` 步交换 `17 维量测 / 5 维设定点（torque, yaw, pitch×3 offset）`。
- **决策**：**主线用文件桥接**（工程已在用、跨平台稳定、无需重编译 DLL）。ZeroMQ 作为可选高频通道，仅当文件 I/O 成为瓶颈时启用（列入 Phase 0 的可选任务）。

### 1.4 FLORIS 侧（代理模型的现成载体）

- 工程含 `wfcrl/simulators/floris/inputs/template/case.yaml`（GCH 模型，NREL 5MW）。**这就是 A/B/C 代理模型的基础**：
  - 已配 `velocity_model: gauss`、`deflection_model: gauss`、`turbulence_model: crespo_hernandez`、`combination_model: sosfs`。
  - 已含待标定参数 `ka=0.38, kb=0.004`（尾流膨胀），`alpha=0.58, beta=0.077`。
  - `cosine_loss_exponent_yaw: 1.88`（与公式推导中 C_P 偏航损失指数 p_P 一致）。
- **须修改项**：`case.yaml` 的 `layout_x / layout_y` 当前是 3 机一行，需改为 **6 机 2×3 @ 4D**（见 §Phase0-M1）。

---

## 2. 场站配置基线（所有方案共享）

| 项目 | 取值 | 说明 |
|---|---|---|
| 机型 | NREL 5MW | D = 126 m，额定 5 MW，轮毂高 90 m |
| 台数 | 6 | 2 行 × 3 列 |
| 间距 | 4D = 504 m | 行、列间距均为 504 m |
| 机位坐标 (x=顺风列向, y=展向行) | 见下 | 以西风 270° 为主风向对齐列方向 |
| 湍流强度 | 0.06（基线，可扫 0.05/0.10） | 与工程 `case.yaml` 一致 |
| 参考风速 | 8 m/s（可扫 6/8/10） | 工程已有 `inflow_6ms/8ms.bts` |

**机位坐标（建议）**：列方向为 x（顺风），行方向为 y。
```
行0: T1(0, 0)      T2(504, 0)      T3(1008, 0)
行1: T4(0, 504)    T5(504, 504)    T6(1008, 504)
```
> 注：FAST.Farm 的 `.fstf` 与 FLORIS 的 `case.yaml` 都要用这套坐标；主风向 270°（西风）时 x 为顺风向，T1/T4 为最上游、T3/T6 为最下游。

---

## 3. 总体模块地图（共用 vs 各方案新增）

```
┌─────────────────────────── Phase 0：三方案共用基础设施 ───────────────────────────┐
│  M1 场站配置与算例生成    M2 FAST.Farm 接口封装(读measurements/写controls)          │
│  M3 感知与数据预处理层    M4 诱导因子↔功率设定点换算    M5 代理模型封装(FLORIS)      │
│  M6 控制基类与指令分配    M7 评估与可视化(功率/CoV/能量)  M8 实验编排与配置系统     │
│  M9 控制模式接口(5 mode，A/B/C 共用)                                                 │
└───────────────────────────────────────────────────────────────────────────────────┘
        │                          │                          │
        ▼                          ▼                          ▼
┌──────────────┐          ┌──────────────────┐        ┌──────────────────────┐
│  方案 A 新增  │          │   方案 B 新增     │        │      方案 C 新增       │
│ A1 参数标定   │          │ B1 FLORIDyn 动态  │        │ C1 灰箱残差/修正场     │
│ A2 稳态重优化 │          │ B2 EnKF 观测器    │        │ C2 联合状态-参数-残差  │
│ A3 A-控制器   │          │ B3 MPC 控制器     │        │ C3 GP/PGNN 代理        │
│ (均支持5 mode)│          │ B4 时延/TWF 处理  │        │ C4 RL策略+安全投影     │
│              │          │ B5 B-控制器       │        │ C5 C-控制器            │
└──────────────┘          └──────────────────┘        └──────────────────────┘
        │                          │                          │
        └──────────────────────────┴──────────────────────────┘
                                    ▼
┌─────────────── 测试 Demo（三方案共用，与 greedy 基线对比）───────────────┐
│  D1 贪婪基线控制器   D2 Demo运行器   D3 对比评估报告   D4 冒烟测试        │
└──────────────────────────────────────────────────────────────────────────┘
```

- **共用（Phase 0，含 M9 控制模式）**：感知、接口、代理模型封装、评估、编排、5 mode 接口——A/B/C 都直接调用，只开发一次。
- **各方案新增**：仅"观测器 + 控制器（+ 动态模型）"部分不同，且都支持 M9 的 5 个控制模式。B 复用 A 的 FLORIS 封装并升级为动态；C 复用 B 的观测器与 MPC 并叠加数据驱动项。
- **测试 Demo（共用）**：贪婪基线 + 运行器 + 对比报告 + 冒烟测试，三方案共用，每个方案都与 greedy 定量对比。

---

# Phase 0：三方案共用基础设施（先开发，只做一次）

> 目标：搭好"感知 → 代理模型 → 优化 → 指令下发 → 评估"的公共骨架，让 A/B/C 只需替换观测器与控制器即可插入。建议放在新目录 `wfcrl/closedloop/`。

## M1　场站配置与算例生成

- **职责**：把 2×3 @ 4D 的 NREL 5MW 布局固化为两套一致的配置：FAST.Farm 算例（`.fstf` + 6 个 OpenFAST 实例 + 每机 `DISCON_T<i>.IN`）与 FLORIS `case.yaml`。
- **挂接点**：
  - 修改 `floris/inputs/template/case.yaml` 的 `layout_x/layout_y` 为 §2 坐标，`turbine_type: nrel_5MW`。
  - 生成/修改 FAST.Farm 主文件与 InflowWind 指向现有 `inflow_*ms.bts`；为 6 机各写一个 `DISCON_T<i>.IN`（首行 = 机组 ID，桥据此区分机组）。
- **输入/输出**：输入布局参数 → 输出成套算例目录 `cases/farm_2x3_4D/`。
- **验收**：FAST.Farm 能在无控制（`controls.txt` 缺省）下跑通 6 机；FLORIS 加载同布局无报错；两侧机位坐标数值一致。

## M2　FAST.Farm 接口封装（核心底座）

- **职责**：把 §1.1/§1.2 的文件协议封装成干净的 Python API，屏蔽文件读写细节。
- **建议 API**：
  ```python
  class FarmBridge:
      def read_measurements() -> dict[int, TurbineMeas]   # 解析 measurements_T*.txt
      def write_controls(step:int, cmds:dict[int, Cmd])   # 写 controls.txt(含END, 递增step)
      def wait_step(step:int, timeout)                     # 轮询直到该 step 的量测就绪
      def reset()                                          # 清理运行目录
  ```
- **关键实现要点**：
  - 写 `controls.txt` 必须**原子写**（先写临时文件再 rename），避免桥读到半行。
  - `step` 单调递增；与 FAST.Farm 全局步 `DT` 对齐（读 `.fstf` 的 DT）。
  - 解析 `measurements_T<i>.txt` 时容错（桥在覆写瞬间可能被读到空）。
- **挂接点**：直接对接 `DISCON_bridge.f90` 的 `controls.txt` / `measurements_T*.txt`。
- **验收**：给 6 机下发一组固定 `yaw`，日志 `measurements` 中 `nacyaw` 收敛到目标；下发 `power=` 后 `genpwr` 跟踪到设定点。

## M3　感知与数据预处理层（观测器的输入前端）

- **职责**：把原始逐步量测转成控制器/观测器可用的"风况估计"。三方案共用。
- **功能**：
  - **时间平均/滤波**：对 `genpwr`、`wind_x` 做滑动平均（默认窗 ~30–60 s），滤除湍流脉动；给出各机功率、机舱风速、机舱风向估计。
  - **各机来流重构**：由 `wind_x` + `nacyaw` 估计各机本地风速/风向（异质来流基础）。
  - **场级风况**：聚合上游机（T1/T4）估计自由流 `U∞, φ`；预留"场站扫描雷达 15 min 更新"的接口（当前用上游机代理）。
  - **载荷监测**：透传 `mip1/moop1/mzb1` 供评估层算疲劳当量。
- **输出**：`FlowEstimate{U_inf, phi, per_turbine:{U_i, phi_i, P_i}, TI_est}`。
- **验收**：稳态风下 `U∞` 估计与设定值误差 < 3%；阶跃风向下估计滞后 < 1 个平均窗。

## M4　诱导因子 ↔ 功率设定点换算（共用工具）

- **职责**：控制器优化输出的轴向诱导 `a_i`，与桥接受的 `power=<MW>`（或 `torque`）互相换算。
- **公式**（贴合工程）：由制动盘理论 `C_P = 4a(1−a)²`，目标功率 `P_i = ½ρA C_P(a_i) U_eff,i³ cos^1.88(γ_i)`；下发 `power = P_i(MW)`。反向由实测 `genpwr` 与 `U_eff` 反推等效 `a`。
- **边界**：`a ∈ [0, 1/3]`（贪心上限 a=1/3）；限幅与速率见 M6。
- **验收**：给定 `a=0.25, U=8, γ=0`，换算功率下发后，稳态 `genpwr` 与理论值误差 < 5%。

## M5　代理尾流模型封装（FLORIS，A/B/C 共用基础）

- **职责**：把 FLORIS 封装为统一调用，供优化器"预测各机功率/有效风速"。
- **建议 API**：
  ```python
  class SurrogateModel:
      def set_conditions(U_inf, phi, TI)
      def set_controls(yaw:np.ndarray, induction:np.ndarray)
      def predict() -> {P_i, U_eff_i, P_farm}
      def set_params(theta:dict)          # ka,kb,alpha,beta... 供方案A/C标定写回
  ```
- **挂接点**：加载 M1 的 `case.yaml`；`set_params` 写 FLORIS 尾流参数。
- **说明**：方案 A 直接用其稳态预测；方案 B 在其上加 OP 平流（见 B1）；方案 C 在其输出上加残差（见 C1）。
- **验收**：6 机 @ 4D、270° 下 FLORIS 预测的尾流序列合理（下游机功率显著低于上游）；`set_params` 改 `ka` 后下游功率单调变化。

## M6　控制基类与指令分配（共用执行层）

- **职责**：定义统一控制器接口 + 工程化的指令后处理。
- **建议 API**：
  ```python
  class BaseController(ABC):
      def __init__(self, mode: ControlMode, ...)    # 控制模式在构造时选定(见M9)
      def step(flow_est, meas) -> dict[int, Cmd]   # 由A/B/C各自实现，内部按mode裁剪决策变量
  class CommandArbiter:
      def apply_limits(cmd)   # 偏航幅值±γmax、速率γ̇max、死区/迟滞；诱导范围与速率
  ```
  > 所有控制器都持有一个 `ControlMode`（M9），`step()` 内部据此决定优化/下发哪些通道。
- **约束**（贴合桥执行方式）：偏航目标限 `|γ|≤30°`，速率经桥的 0.2 比例已自然限速，另加控制侧步进限幅；降功率经 `power=` 限 `[P_min, P_rated]`。
- **验收**：注入抖动指令，经 Arbiter 后 `nacyaw` 无高频往复；切换指令平滑。

## M7　评估与可视化（共用，闭环收益的度量）

- **职责**：统一口径对比各方案与开环基线，产出图表。
- **指标（公式对齐推导文档 §4.4）**：
  - 场级功率增益 `ΔP% = (P_ctrl − P_greedy)/P_greedy`。
  - 功率脉动 `CoV_i = σ(P_i)/mean(P_i)`（越过启动暂态后统计）。
  - 窗口能量 `E = Σ P_farm·Δt`。
  - 载荷代理：叶根弯矩当量（由 `moop1/mip1`）。
- **基线**：贪心（全 0 偏航、a=1/3）与**现有开环 FLORIS-LUT**。
- **验收**：对同一工况序列，能一键出"闭环 vs 基线"的功率时序、增益柱状、CoV 对比图。

## M8　实验编排与配置系统（共用）

- **职责**：串起"起 FAST.Farm → 起控制器循环 → 采集 → 评估"，用配置文件描述实验。
- **功能**：
  - 单一 `experiment.yaml` 指定：布局、风况序列（U/φ/TI 时序）、方案(A/B/C)、控制周期、时长、随机种子。
  - 进程编排：并行启动 FAST.Farm 与 Python 控制循环（参考工程内 `_compile.py`/`compile.bat` 的构建流程先编好桥 DLL）。
  - 日志与断点续跑。
- **验收**：改 `experiment.yaml` 的 `scheme: A/B/C` 即可切换方案跑完整实验并落盘结果。

## M9　控制模式接口（5 mode，A/B/C 共用）　`control_mode.py`

- **职责**：给三种控制器统一增加"控制模式"选择。模式定义**对齐工程实际可控通道**——ZeroMQ 接口 `setpoints(5)` 与文件桥 `cmd_*` 字段共同支持的 5 个执行自由度：`torque(1) / yaw(2) / pitch(3–5)`，以及偏航+降功率的组合。A/B/C 的优化器都据此决定"优化哪些变量、下发哪些字段"。
- **5 个控制模式定义**（工程内 `ZeroMQInterface.f90` 的 5 设定点 + `DISCON_bridge.f90` 的 yaw/pitch/torque/power 落地）：

  | mode | 名称 | 优化变量 | 下发字段(controls.txt) | 说明 |
  |---|---|---|---|---|
  | 1 | 纯偏航 (YAW) | `γ_i` | `yaw=` | 只做尾流偏转，诱导固定为贪心 a=1/3 |
  | 2 | 纯转矩降功率 (TORQUE) | `a_i`→转矩 | `torque=` | 只做转矩侧诱导控制，偏航置 0 |
  | 3 | 纯变桨降功率 (PITCH) | `a_i`→桨距 | `pitch=` | 只做变桨侧诱导控制，偏航置 0 |
  | 4 | 偏航+转矩 (YAW_TORQUE) | `γ_i, a_i` | `yaw=` + `torque=` | 偏转与转矩降功率联合 |
  | 5 | 偏航+变桨 (YAW_PITCH) | `γ_i, a_i` | `yaw=` + `pitch=` | 偏转与变桨降功率联合 |

  > 说明：mode 2/4 的"诱导"经 M4 换算为 `power=`（桥内功率闭环，比裸 `torque=` 更稳，推荐）或直接 `torque=`；mode 3/5 的"诱导"换算为集体桨距 `pitch=`。偏航在所有含偏航的模式里都走 `yaw=`（桥内比例速率执行）。若后续启用 ZeroMQ，则映射到 `setpoints(2)=yaw, setpoints(1)=torque, setpoints(3:5)=pitch`。

- **建议 API**：
  ```python
  class ControlMode(Enum):
      YAW = 1; TORQUE = 2; PITCH = 3; YAW_TORQUE = 4; YAW_PITCH = 5

  class ControlModeSpec:
      # 由 mode 推导：哪些是决策变量、哪些置默认、下发映射到哪些字段
      def decision_vars(mode) -> list[str]        # e.g. ['yaw'] / ['yaw','a']
      def fixed_defaults(mode) -> dict            # e.g. {'a': 1/3, 'yaw': 0}
      def to_controls(mode, yaw_i, a_i, U_eff_i) -> Cmd   # 组装controls.txt字段
      def bounds(mode) -> dict                    # 决策变量的约束(供优化器)
  ```
- **与三控制器的接线**（关键：一处定义、三处复用）：
  - **方案 A**：`steady_opt` 的决策变量集合与目标由 `ControlModeSpec.decision_vars(mode)` 决定；mode=1 时只优化 γ、a 固定 1/3，mode=2/3 时只优化 a、γ 固定 0，mode=4/5 时联合优化。
  - **方案 B**：`MPC` 的决策序列维度按 mode 裁剪（如 mode=1 只有偏航序列，省去诱导维度，降维加速）；FLORIDyn 预测时对未优化变量用 `fixed_defaults`。
  - **方案 C**：同 B；RL 策略的动作空间按 mode 定义（mode=1 动作=偏航增量，mode=4 动作=偏航+诱导）。
- **输入/输出**：输入 `mode` + 优化解 `(γ_i, a_i)` + 各机 `U_eff_i` → 输出组装好的 `Cmd`（供 M6 分配、M2 下发）。
- **验收**：
  - 5 个 mode 各自下发后，`measurements` 反映预期：mode=1 只有 `nacyaw` 变、功率不主动降；mode=2/3 只有功率降（偏航≈0）；mode=4/5 偏航与功率同时变。
  - 切换 mode 时决策变量维度正确变化（日志打印优化变量集合）。
  - mode=1 且全场 0 偏航 = 贪婪基线（与 §Demo 的 baseline 一致）。

### Phase 0 交付物清单
- `wfcrl/closedloop/` 包骨架：`bridge.py(M2) sensing.py(M3) induction.py(M4) surrogate.py(M5) base_controller.py(M6) evaluate.py(M7) runner.py(M8) control_mode.py(M9)`
- `cases/farm_2x3_4D/`（M1）
- 一个"直连测试"：固定偏航/降功率下发，验证闭环读写通路（不含任何智能控制）。

---

# 方案 A：FLORIS 再标定 + 稳态重优化

> 定位：**最小闭环、快速见效、验证管线**。复用 Phase 0 全部模块，仅新增"周期性参数标定"观测器与"稳态重优化"控制器。对应推导文档第 1 章、公式 (1.1)–(1.11)。

## A0 依赖
- 复用：M2 接口、M3 感知、M4 换算、M5 FLORIS 封装、M6 分配、M7 评估、M8 编排。
- 新增目录：`wfcrl/closedloop/scheme_a/`

## A1　参数标定模块（观测器）　`calibration.py`
- **职责**：每 `T_cal`（默认 10–15 min 仿真时）用窗口内实测标定 FLORIS 参数，把模型校到当前场况。
- **输入**：M3 给出的各机 10-min 平均功率 `P̄_i`、机舱风速/风向；当前 `case.yaml` 参数。
- **待标定参数**（对齐 case.yaml）：`θ = [ka, kb, alpha, beta, {φ_i^bias}, U∞]`（公式 1.7）。
- **算法**：
  1. 构造加权最小二乘目标（公式 1.8）：`min_θ Σ w_i (P̄_i^meas − P_i^FLORIS(θ))² + λ‖θ−θ₀‖²`。
  2. 可选用能量比作观测量（公式 1.9）抵消风况分布偏差。
  3. **可辨识性处理**：对雅可比做 SVD，仅更新奇异值 > ε 的分量（公式 1.10），防病态。
  4. 用 `scipy.optimize`（SLSQP/最小二乘）求解；`M5.set_params(θ̂)` 写回。
- **输出**：标定后的 `θ̂`（落盘 + 更新 surrogate）。
- **验收**：人为把 FLORIS 的 `ka` 设偏，标定后能拉回到使 6 机功率预测误差最小的值；标定前后下游机功率预测 RMS 误差下降。

## A2　稳态重优化模块（控制器核心）　`steady_opt.py`
- **职责**：在标定后的 FLORIS 上，对当前风况箱重解全场偏航 + 诱导最优。
- **算法**（公式 1.11）：`max_{γ,a} P_farm(γ,a; θ̂)`，约束 `|γ_i|≤30°, a_i∈[0,1/3]`。
  - 实现两条路径，二选一或对比：**(a) SLSQP** 梯度法；**(b) Serial-Refine / 广义 SR** 确定性逐机精化（对偏航多峰更稳，推荐作默认）。
  - 6 机维度低，单次优化秒级，可每个控制周期重跑或缓存为小型 LUT。
- **输出**：`{γ_i*, a_i*}` → 经 M4 换算 → M6 分配 → M2 下发。
- **验收**：270° 下优化给出上游机正偏航、下游机近 0 的合理模式；相对贪心基线在 FLORIS 内预测有正增益。

## A3　方案 A 控制器封装　`controller_a.py`
- **职责**：实现 `BaseController.step()`，编排"（到标定周期则）A1 标定 → A2 重优化 → 下发"。**支持 M9 的 5 个 mode**：A2 优化的决策变量由 `mode` 裁剪。
- **逻辑**：
  ```
  __init__(mode): self.spec = ControlModeSpec(mode)
  每控制步:
    flow = sensing.estimate()
    if 到达 T_cal: theta = calibration.run(window_meas); surrogate.set_params(theta)
    if 到达 T_ctrl 或风况箱切换:
        vars = self.spec.decision_vars(mode)          # ['yaw'] / ['a'] / ['yaw','a']
        (γ*,a*) = steady_opt.solve(flow, surrogate, vars, fixed=self.spec.fixed_defaults(mode))
    cmds = arbiter.apply_limits(self.spec.to_controls(mode, γ*, a*, flow.U_eff))
    bridge.write_controls(step, cmds)
  ```
- **验收**：完整跑通"标定→重优化→下发"闭环；日志显示标定周期触发、指令更新；**切换 5 个 mode 均能正确优化对应变量并下发对应字段**。

## 方案 A 里程碑
1. **A-M1**：A2 单独跑（不标定，用默认 FLORIS）→ 打通"优化→下发→FAST.Farm 执行"，对比贪心。
2. **A-M2**：接入 A1 标定 → 验证标定改善预测。
3. **A-M3**：完整方案 A + M7 评估，出"A vs 贪心 vs 现有开环LUT"对比报告。

## 方案 A 风险与注意
- 稳态模型**不含时延**，强非定常工况增益有限——这是预期局限，作为 B 的基线即可。
- 标定窗口内风况须相对平稳，否则标定被"平均掉"；编排上选准静态工况做 A 的验证。

---

# 方案 B：FLORIDyn 动态模型 + EnKF 观测器 + MPC 控制器

> 定位：**推荐主方案**。显式处理尾流平流时延与各机来流异质。复用 Phase 0 + 方案 A 的 FLORIS 封装（升级为动态），新增动态模型、EnKF、MPC 三大块。对应推导文档第 2 章、公式 (2.1)–(2.27)。

## B0 依赖
- 复用：Phase 0 全部；M5 的 FLORIS 封装作为 FLORIDyn 的"局部稳态求解器"。
- 新增目录：`wfcrl/closedloop/scheme_b/`
- **建议**：优先复用开源 **FLORIDyn / OFF** 框架（Becker 等）作为 B1 的实现底座，减少造轮子；若自研，按下述状态方程实现。

## B1　FLORIDyn 动态尾流模型　`floridyn_model.py`
- **职责**：给 FLORIS 加"观测点(OP)平流"动态，显式表达时延与异质。
- **核心实现**（公式 2.1–2.9）：
  - **OP 状态**：每机发射 OP，记录 `[x,y,U,φ,γ,a]`（公式 2.1）。
  - **平流**：OP 沿当地风向以当地风速下传，显式欧拉离散（公式 2.3）；用移位寄存器实现（公式 2.4）——每步 OP 前移一格、队首注入当前机组状态。
  - **时延**：`τ_{j→i} = d_{j→i}/U_adv`（公式 2.5），由 OP 到达下游的步数体现。
  - **临时风场 TWF**：评估某机时取邻域 OP 组装局部准均匀风场，调 M5 的 FLORIS 算局部亏损，用 OP 携带的**历史控制量** `γ_j^{t−τ}`（公式 2.6–2.7）。
  - **异质**：各机 `U_amb,i, φ_amb,i` 独立，不假设全场均匀。
- **接口**：
  ```python
  class FLORIDyn:
      def step(dt, controls)          # 推进OP + 更新场
      def get_measurements() -> {P_i, U_eff_i}   # 供EnKF的h(·)
      def predict(horizon, control_seq) -> P_farm_traj  # 供MPC滚动
      def state / set_state()          # 供EnKF读写
  ```
- **验收**：对 T1→T2（4D）阶跃偏航，下游 T2 功率变化**滞后 τ≈504/U_adv 秒**再显现（时延被正确建模）；6 机稳态收敛值与 FLORIS 一致。

## B2　EnKF 观测器　`enkf.py`
- **职责**：每控制步用实测校正 FLORIDyn 整场流态与参数，输出带不确定度的状态。
- **核心实现**（公式 2.11–2.18）：
  1. **集合**：维护 `N_e`（默认 40–60）个状态样本（公式 2.11）。
  2. **预测步**：每样本经 `FLORIDyn.step` 推进 + 过程噪声（公式 2.12–2.14）。
  3. **更新步**：由集合估互协方差 `P^xy`、自协方差 `P^yy`（公式 2.15–2.16），算增益 `K=P^xy(P^yy)⁻¹`（2.17），扰动观测更新每样本（2.18）。
  4. **量测向量**（公式 2.19）：各机 `genpwr`（必用）+ `wind_x`(机舱风速) + `nacyaw`；预留扫描雷达场级风向并入。
  5. **联合参数估计**（公式 2.20）：把尾流参数 θ 增广进状态做随机游走，实现"模型在线修正 = 状态观测"统一。
- **数值稳定**：协方差膨胀 + 局部化（localization）抑制小样本伪相关与发散。
- **输入来源**：M3 感知层（滤波后的量测）。
- **验收**：仅用各机功率量测，EnKF 能把人为偏置的背景风速/风向估计拉回真值；相对 FLORIDyn 开环，状态 RMS 误差下降（复现文献 ~20% 量级改善的定性趋势）。

## B3　MPC 控制器　`mpc.py`
- **职责**：以 EnKF 后验为初值，在预测时域内滚动优化偏航/诱导序列，最大化预测发电量。
- **核心实现**（公式 2.21–2.27）：
  - **目标**（公式 2.21）：`max Σ_{j=0}^{Np-1} Σ_i P_i · Δt − Σ_{Nc} (λ_γ‖Δγ‖² + λ_a‖Δa‖²)`。
  - **时延约束（关键）**（公式 2.22）：`N_p·Δt > τ_max`，`τ_max` 由最大间距/最低风速估（6 机 4D、6 m/s 下 τ_max≈两跨×504/6）。**预测时域必须覆盖尾流穿越，否则控制器"看不到"下游收益**。
  - **预测**（公式 2.23）：用 `FLORIDyn.predict` 滚动，初值锚定 EnKF 后验。
  - **约束**（公式 2.24）：偏航幅值/速率、诱导范围/速率。
  - **滚动**（公式 2.25）：执行首段、其余重解。
  - **降维/加速**（公式 2.26–2.27）：偏航序列用 B 样条基降维；梯度用自动微分或伴随，供 SQP/内点法。
- **实时性**：控制周期 `T_ctrl`（10–60 s）内完成 EnKF 更新 + 一次 MPC 求解；调 `N_e`、`N_p`、样条基数平衡精度与耗时。
- **验收**：在含风向缓变/阶跃的时序上，MPC 相对贪心与方案 A 在**过渡态**有额外增益；无高频抖动。

## B4　时延与异质处理集成　（分散在 B1/B2/B3，单列以强调验收）
- **时延双保险**：模型内嵌（B1 的 OP 历史状态）+ 时域覆盖（B3 的 `N_p·Δt>τ_max`）。
- **异质**：B1 逐机 TWF + B2 逐机背景风估计。
- **专项验收**：构造"上游 T1 偏航阶跃"实验，确认 (i) 下游响应有正确时延；(ii) MPC 的预测时域足够长以在偏航前就规划到时延后的收益。

## B5　方案 B 控制器封装　`controller_b.py`
- 实现 `BaseController.step()`：`sensing → EnKF.update(meas) → MPC.solve(x̂⁺, mode) → 下发`。
- **支持 M9 的 5 个 mode**：MPC 的决策序列维度按 `mode` 裁剪（mode=1 只含偏航序列，省诱导维度→降维加速；mode=2/3 只含诱导序列；mode=4/5 联合）；FLORIDyn 预测时对未优化变量用 `fixed_defaults`。
- **验收**：完整闭环跑通；日志显示每步 EnKF 校正量与 MPC 解；**5 个 mode 下 MPC 决策维度正确变化且约束满足**。

## 方案 B 里程碑
1. **B-M1**：B1 FLORIDyn 单独验证（时延、稳态一致性），离线对拍 FAST.Farm 的下游响应时延。
2. **B-M2**：B2 EnKF 接入（用 FAST.Farm 量测校正 FLORIDyn），验证状态收敛。
3. **B-M3**：B3 MPC 接入，形成 EnKF+MPC 闭环；先定常风调通，再上时变风。
4. **B-M4**：完整方案 B + 评估，出"B vs A vs 贪心"在时变工况的增益对比（重点看过渡态）。

## 方案 B 风险与注意
- **实时性**是主要风险：`N_e × N_p` 直接决定耗时。先用小集合/短时域调通，再逐步加大；必要时切 ZeroMQ 高频通道或降低控制频率（分钟级动作对尾流控制通常够用）。
- **量测噪声/偏置**侵蚀增益：对 `nacyaw` 偏置单独建模（并入 θ）；`R` 按 `wind_x` 与 `genpwr` 的实际噪声定。
- **FLORIDyn 与 FAST.Farm 的模型差**：EnKF 正是用来吸收它的；若残差仍大，转入方案 C 的残差修正。

---

# 方案 C：灰箱学习增强 / 数字孪生

> 定位：**增量提升，B 成熟后再做**。在 A 或 B 的物理代理上叠加数据驱动残差，纠正连"调参后物理模型"也表达不了的结构性误差。对应推导文档第 3 章、公式 (3.1)–(3.8)。

## C0 依赖
- 复用：Phase 0 全部 + 方案 B 的 FLORIDyn/EnKF/MPC（推荐在 B 之上叠加）。
- 新增目录：`wfcrl/closedloop/scheme_c/`
- **前置条件**：需已积累一定量 FAST.Farm 运行数据（用 A/B 阶段的实验落盘数据作训练集）。

## C1　灰箱残差 / 修正场模块　`residual_model.py`
- **职责**：学习"物理代理预测"与"FAST.Farm 实测"之间的系统残差。
- **两种形态**（公式 3.1–3.2）：
  - **加性残差**：`U_eff^corr = U_eff^phys(θ) + g_ψ(ξ)`，`ξ` = 风况/机位/邻机偏航等特征。
  - **空间修正场**：`U_amb(p) = U_amb⁰·(1+c(p))`，`c(p)=Σβ_r Φ_r(p)`（基函数展开，可解释）。
- **验收**：在留出工况上，物理+残差的功率预测误差显著低于纯物理模型。

## C2　联合状态-参数-残差估计　`joint_estimator.py`
- **职责**：把物理参数 θ、修正场系数 β、残差网络参数 ψ 一并辨识。
- **实现**（公式 3.3–3.5）：
  - 批量联合最小化（离线/准静态，公式 3.3），是方案 A 标定的推广。
  - 可选高斯过程残差（公式 3.4–3.5）给出预测方差，供 C4 安全护栏。
- **验收**：联合估计后模型在多工况上误差一致下降；GP 方差在数据稀疏区合理增大。

## C3　GP / PGNN 代理（可选替代）　`ml_surrogate.py`
- **职责**：若残差修正不足，用高斯过程或物理引导神经网络(PGNN)做更准的代理直接进 MPC。
- **实现**：PGNN 损失含物理残差正则（公式 3.6），兼顾拟合与物理一致。
- **验收**：作为 B3-MPC 的预测模型替换项，闭环增益不低于 B 且在极端工况更稳。

## C4　RL 策略 + 安全投影（可选，激进路线）　`rl_policy.py`
- **职责**：离线预训练策略 `π_φ: s→u` 换取在线实时性；在线只做安全微调。
- **实现**（公式 3.7–3.8）：
  - 策略优化最大化折扣累计奖励（功率归一化 − 动作惩罚，公式 3.7）。**可直接复用 WFCRL 本身的 RL 环境接口**（工程本就是 RL benchmark）。
  - **安全护栏（必须）**：把策略输出投影到物理模型认可的安全集（公式 3.8）——只接受"物理模型评估功率不低于基线(1−ε)倍"的动作，否则回退物理最优。
- **验收**：策略在安全投影下永不低于物理基线；在训练覆盖工况上超过 B。

## C5　方案 C 控制器封装　`controller_c.py`
- 两条路线择一：**(i) 灰箱 MPC** = B5 的 MPC 换用 C1/C3 的模型；**(ii) RL** = 用 C4 策略 + 安全投影。
- **支持 M9 的 5 个 mode**：灰箱 MPC 路线同 B5 按 mode 裁剪决策变量；RL 路线的动作空间按 mode 定义（mode=1 动作=偏航增量，mode=4/5 动作=偏航+诱导增量）。
- **验收**：完整闭环；与 B 对比出增益上限；**5 个 mode 均可运行**。

## 方案 C 里程碑
1. **C-M1**：用 A/B 落盘数据训练 C1 残差，离线验证预测改善。
2. **C-M2**：C2 联合估计接入闭环（灰箱 MPC 路线）。
3. **C-M3**（可选）：C4 RL + 安全投影，复用 WFCRL 环境预训练。
4. **C-M4**：完整方案 C + 评估，出"C vs B"上限对比。

## 方案 C 风险与注意
- **依赖数据量与覆盖度**：训练工况未覆盖处外推有风险——安全投影(3.8)是硬性要求，物理模型必须兜底。
- 建议**先灰箱残差(C1/C2)、后 RL(C4)**：前者风险低、可解释；后者上限高但需大量训练。

---

# 4. 跨方案集成、时序与验证策略

## 4.1 三方案的复用关系（避免重复开发）
| 层 | 方案 A | 方案 B | 方案 C |
|---|---|---|---|
| 整体模型 | M5 FLORIS(稳态) | B1 FLORIDyn(=M5+OP平流) | C1/C3(=B1+残差/ML) |
| 观测器 | A1 批量标定 | B2 EnKF | C2 联合估计(=A1推广+B2) |
| 控制器 | A2 稳态重优化 | B3 MPC | C5 灰箱MPC/RL(=B3+ML) |
| 控制模式(5 mode) | **M9（三方案共用）** | ← | ← |
| 感知/接口/评估 | **Phase 0（三方案完全共用）** | ← | ← |
| 测试 Demo + greedy 基线 | **D1–D4（三方案共用）** | ← | ← |

> 关键：**B 的 FLORIDyn 复用 A 的 FLORIS 封装；C 复用 B 的 EnKF+MPC；5 mode(M9) 与 Demo(D1–D4) 三方案共用。** 每个方案只新增 2–5 个算法模块，其余全部复用。

## 4.2 两个时间尺度（与 FAST.Farm DT 对齐）
| 回路 | 更新对象 | 典型步长 | 实现位置 |
|---|---|---|---|
| 快回路 | EnKF 校正 / OP 平流 / 量测采集 | Δt ≈ 对齐 FAST.Farm DT（如 2 s） | M2/M3、B1/B2 |
| 控制回路 | 优化重解 / 指令下发 | T_ctrl ≈ 10–60 s | A2/B3/C5 |
| 慢回路 | 参数标定 / 场级修正 | T_cal ≈ 10–15 min | A1/C2 |

## 4.3 统一验证协议（所有方案同口径）
1. **基线**：贪心（0 偏航、a=1/3）+ 现有开环 FLORIS-LUT。
2. **工况**：
   - 定常风（U=8, φ=270°, TI=0.06）——验证正确性与稳态增益。
   - 时变风（φ 缓变 ±10°、阶跃；U 6→10 过渡）——验证过渡态增益（B/C 的主场）。
   - TI 扫描（0.05/0.10）——验证鲁棒性。
3. **指标**：ΔP%、CoV、窗口能量、叶根弯矩当量（M7 统一出图）。
4. **专项**：时延实验（上游偏航阶跃看下游滞后）——验证 B 的时延处理。
5. **鲁棒性**：量测注入噪声/偏置，复现增益衰减敏感性，据此定传感器精度与滤波参数。

## 4.4 建议开发时间线（供排期参考，非承诺）
```
Phase 0    ██████████                     共用底座(M1-M9，含5 mode)，最重要，先做透
Demo D1-D4    ████                         贪婪基线+运行器+报告(与Phase0并行/紧随)
方案 A          ████████                    最小闭环，打通管线，出A vs greedy报告
方案 B                 ████████████████     主方案，投入最大，出B vs A vs greedy
方案 C                             ████████ 增量，依赖B与数据，出C vs B报告
```
- **Phase 0（含 M9 控制模式）未完成前不要开始 A**：接口/感知/评估/模式是三方案地基。
- **Demo(D1–D4) 紧随 Phase 0**：贪婪基线与运行器要在方案 A 验收前就绪，否则没有对比基准。
- **A 完成后再开 B**：A 验证了读写通路、评估口径与 Demo 流程，B 专注算法。
- **C 最后做**：需要 A/B 阶段的数据与稳定的 B 闭环。

---

# 5. 控制器测试 Demo 开发方案（验证 FAST.Farm 运行效果 + 贪婪基线对比）

> 目标：为 A/B/C 三种控制器提供一套**统一的测试 demo**，在真实 FAST.Farm 上跑通并**与贪婪控制基线定量对比**，验证闭环控制的实际效果。Demo 建在 Phase 0 之上，是每个方案里程碑的"验收出口"。建议放在 `wfcrl/closedloop/demos/`。

## 5.1 Demo 的定位与设计原则
- **单一入口、可切换**：一个 `run_demo.py`，通过参数选择 `--controller {greedy,A,B,C}`、`--mode {1..5}`、`--case`、`--wind`，跑完自动出对比报告。
- **贪婪基线是一等公民**：`greedy` 作为内置控制器（全场 0 偏航、诱导 a=1/3，即 mode=1 且不动作），所有方案都与它对齐比较。
- **同种子、同工况、同时长**：保证 A/B/C 与 greedy 在**完全相同的风况序列与随机种子**下运行，差异才可归因于控制策略。
- **短、可复现**：demo 用短时长（如仿真 600–1200 s）快速验证；长时评估交给 M8 的完整实验。

## 5.2 Demo 模块清单（新增）

### D1　贪婪基线控制器　`demos/greedy_baseline.py`
- **职责**：实现最简基线——所有机组偏航对准来流（γ=0）、诱导取贪心最优 a=1/3（不做尾流控制）。
- **实现**：`GreedyController(BaseController)`，`step()` 恒定下发 `T<i> yaw=0 power=P_greedy_i`（`P_greedy_i` 由 M4 用 a=1/3 与实测 `U_eff_i` 算）。
- **为何必要**：它是"不控制"的对照，衡量任何方案的**净增益**（ΔP% 的分母）。也是 mode=1 零动作的退化情形，可交叉校验 M9。
- **验收**：6 机 @ 4D、270° 下贪婪基线功率稳定；下游机因尾流明显低于上游机（尾流效应存在，才有控制空间）。

### D2　Demo 运行器　`demos/run_demo.py`
- **职责**：编排单次 demo——起 FAST.Farm → 起选定控制器循环 → 采集 → 存轨迹。
- **参数**：
  ```
  --controller {greedy, A, B, C}   # 选控制器
  --mode {1,2,3,4,5}               # 选控制模式(greedy固定mode=1零动作)
  --case farm_2x3_4D               # 场站算例(M1)
  --wind steady_8ms | ramp | step  # 风况序列
  --duration 1200 --seed 0
  ```
- **实现**：复用 M2 接口、M8 编排；把每步 `(step, t, 各机功率/偏航/风速/载荷, 下发指令)` 落盘为 `trajectory_<controller>_<mode>.parquet`。
- **验收**：任一 `--controller` 都能跑完并落盘轨迹；greedy 与方案共用同一 FAST.Farm 算例与风况。

### D3　对比评估与报告　`demos/compare_report.py`
- **职责**：读入 `greedy` 与 `{A|B|C}` 的轨迹，按 M7 指标做**并排对比**并出图/表。
- **核心对比内容**：
  - **场级功率时序**：baseline vs 方案，叠加同一张图（含启动暂态标注）。
  - **净增益** `ΔP% = (E_ctrl − E_greedy)/E_greedy`（窗口能量比，越过暂态后统计）。
  - **逐机功率对比柱状**：看增益来自哪些机组（上游让利、下游获益）。
  - **功率脉动 CoV** 对比：验证控制未显著恶化脉动。
  - **载荷代理**：叶根弯矩当量对比（偏航/降功率的载荷代价）。
  - **控制动作轨迹**：各机偏航角/功率设定点随时间（确认动作平滑、约束满足）。
- **输出**：一份 `demo_report_<controller>_vs_greedy.html`（或 md + png），含上述图表与一句话结论（净增益 X%）。
- **验收**：对同一工况，报告能清晰给出"方案相对 greedy 的净增益"及分解。

### D4　冒烟测试与断言　`demos/test_smoke.py`
- **职责**：把 demo 变成**可自动判定通过/失败**的测试，供 agent 每次改动后回归。
- **断言项（示例）**：
  - 连通性：`controls.txt` 被写、`measurements_T*.txt` 有更新，`nacyaw` 跟踪到下发目标（误差 < 1°）。
  - 基线合理性：greedy 下游机功率 < 上游机功率（尾流存在）。
  - **效果断言**：在标准偏航工况（如 270°、8 m/s、mode=1 或 4），方案的 `ΔP% ≥ 0`（不劣于基线）；理想情况下游机功率相对 greedy 提升。
  - 稳定性：无 NaN、无发散、偏航速率不越限。
- **验收**：`pytest demos/test_smoke.py` 全绿即认为该控制器在 FAST.Farm 上"跑通且不劣于基线"。

## 5.3 Demo 标准剧本（三方案通用）

| 剧本 | 风况 | 目的 | 期望结果 |
|---|---|---|---|
| S1 连通性 | 定常 8 m/s / 270° | 验证读写通路 | 偏航/功率下发生效，无发散 |
| S2 静态增益 | 定常 8 m/s / 270° | 对比 greedy | 方案 ΔP% ≥ 0（偏航模式下游获益） |
| S3 偏航扫描 | 定常，人工设不同上游偏航 | 验证尾流偏转趋势 | 下游功率随上游偏航呈单峰 |
| S4 时变过渡 | 风向 270°→260° 阶跃/缓变 | B/C 主场 | 过渡态 B/C 优于 A 与 greedy |
| S5 五模式 | 定常，遍历 mode 1–5 | 验证 M9 | 各 mode 行为符合预期、均不劣于 greedy |

> greedy 在每个剧本里都跑一遍作对照；S2/S4/S5 的核心产出就是"方案 vs greedy"的净增益。

## 5.4 各方案如何接入 Demo（复用关系）
- **共用**：D1 贪婪基线、D2 运行器、D3 报告、D4 冒烟测试——**开发一次，三方案共用**。
- **方案 A**：里程碑 A-M3 用 `--controller A` 跑 S1/S2/S3/S5，出"A vs greedy"报告。
- **方案 B**：里程碑 B-M4 用 `--controller B` 跑 S1–S5（**重点 S4 时变过渡**），出"B vs A vs greedy"报告。
- **方案 C**：里程碑 C-M4 用 `--controller C` 跑 S2/S4/S5，出"C vs B vs greedy"上限对比。

## 5.5 Demo 交付物清单
- `demos/greedy_baseline.py(D1) run_demo.py(D2) compare_report.py(D3) test_smoke.py(D4)`
- 每方案至少一份 `demo_report_<X>_vs_greedy`（含净增益结论）。
- `pytest` 冒烟测试纳入 CI/回归。

---

# 6. 给 Agent 的交付建议（逐模块开发）

1. **按模块粒度派单**：每个 M/A/B/C 模块是一个独立任务，附本文件对应小节 + 推导文档公式编号。
2. **每模块必须带单测**：
   - M2：读写 `controls.txt`/`measurements_T*.txt` 的往返测试。
   - M5/B1：给定输入的功率预测回归测试。
   - B2：合成数据下 EnKF 的状态收敛测试。
   - B3：小规模 MPC 的最优性/约束满足测试。
3. **先"直连测试"再"智能控制"**：Phase 0 完成即做固定指令下发的闭环连通性测试（Demo 的 S1 剧本），排除接口问题后再上算法。
4. **复用优先**：开发 B 时明确"FLORIDyn 复用 M5"；开发 C 时明确"复用 B2/B3"。避免重复实现。控制模式(M9)与 Demo(D1–D4) 都是**三方案共用、只开发一次**。
5. **配置驱动**：所有实验经 `experiment.yaml`（M8）切换方案与工况，保证可复现、可对比。
6. **每个方案里程碑以 Demo 报告收口**：A/B/C 每完成一版，用 §5 的 Demo 跑"方案 vs greedy"并出净增益报告，作为该里程碑的验收证据。
7. **控制模式贯穿**：M9 先于 A/B/C 的控制器开发（或与 A 同期），A2/B3/C5 的优化器从一开始就按 `mode` 裁剪决策变量，避免后期返工。

---

## 附：关键接口速查（开发时高频查阅）

**指令下发** `controls.txt`（每步递增 step）：
```
step=<N>
T1 yaw=<deg> power=<MW>      # 偏航目标角 + 功率设定点(表达诱导因子)
...  (T1..T6)
END
```
**量测读取** `measurements_T<i>.txt`：
```
step= t= genpwr(kW) genspd(rpm) gentq rotspd
wind_x(HorWindV m/s)          # 机舱风速 → 观测器输入
blpitch(deg) nacyaw(deg)      # 偏航反馈
mip1 moop1 mzb1(kNm)          # 载荷
```
**代理模型** `case.yaml`：改 `layout_x/y` 为 2×3@4D；标定参数 `ka,kb,alpha,beta`；`nrel_5MW`。
**构建桥 DLL**：先用工程内 `src/compile.bat` / `_compile.py` 编好 `DISCON_WT<i>.dll`（每机 `DISCON_T<i>.IN` 首行填机组 ID）。
